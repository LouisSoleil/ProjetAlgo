Projet algo

carte :
	init : ne peut pas renvoyer vide
	valeur défense : di pas si on renvoie la valeur d’attaque offensive ou défensive
	pointDegat ? int en entrée ?
	ajoutPointDegat : 2 Int ?
	peutAttaquer : Renvoie carteProtocol ou Bool ?
	capturerCarte : être capturée
	cbd et royaume et main : si pas précondition : erreur ?
	royaume : pourquoi renvoie Self ?
	
position :
	carte : on fait quoi si la position est vide ?
	cdb : il faut alors mettre le cdb dans le init() ? sinon on ne peut pas savoir

champsdebataille : Si collection, alors iterateur (position)
	init() : n’a pas besoin du joueur ?
	ajouterUneCarte : pre : ?
	supprimerCarte : nous donne la position ou la carte ?
						renvoie erreur ?
						met la carte dans le royaume ?
	avancerCarte : Precondition : la carte doit être derriere ?
									Ne peut etre une chaine vide ?
	estVidePosition : position est un String ? pas un position protocol ?
	position : pas bon renvoi : ici, c’est une liste swift, pas un tableau
				renvoie A1,A2,… F2,F3 ?
				est tout le temps la même chose ?
	royaume : NE RENVOIE PAS SELF !!!
	cimetiere : Carte ou position ? ne respecte pas la specification
	envoyerCarte : pre et post ? position vide, carte à bon propriétaire ?
                                  position
	PAS AFFICHER !! C’EST LE MAIN !!!

lamain : si collection (de carte), alors iterateur 
	init() : crée une main avec une main de depart dedans ou juste un roi ERREUR ?
	poserCarte : pas deja dans champs de bataille ?
	ListeCarteMain : NON, collection, il faut un iterateur : peut être
	ajouterCarteMain : pré ? modifie la carte ?
	avoirMain : r est un role (a-t-on ce role dans la main)
	
pioche : Iterateur ?
	piocher : iterateur sur les cartes ?
				soit 1 soit 3 : nb de cartes
				NbRole de cette carte incremente 
				pioche a un ordre 

Royaume :
	envoyerCarte : qu’est ce que ça fait ? a besoin d’une main ? String ?
	decrireRoyaume : Quoi ? est pas l’iterateur ? ? 

ItRoyaume : Sers a quoi ?
	init() : C’est le makeItRoyaume de Royaume
	next : renvoie une Carte, pas un Self.Element ?

main : 
	attaquer : nom.pos et nom.p ?
				cdb.avancerCarte ?

+ Test Unitaires :
	ca peut pas etre vide !! c’est pas son type !! carte n’est pas un type optionnel

hugo.brando@etu.umontpellier.fr